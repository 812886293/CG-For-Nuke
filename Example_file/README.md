# 示例文件说明

这里存放的是maya原始渲染文件，maya版本为2024，格式为mb，主要是为了查询aov组合。

另外两个exr文件则是渲染的文件，格式为exr，里面存储了aov通道，两个文件分别是Arnold和redshift渲染器渲染的文件。
文件名分别是masterLayer_arnold_test.exr和masterLayer_redshift_test.exr。

